import { AdminRedirect } from "@/components/admin/admin-redirect"

export default function AdminVerificationPage() {
  return <AdminRedirect targetTab="verification" pageName="Startup Verification" />
}